# 04 - حالة المشروع قبل بدء المهمة (Before State)

## 1. مسار الواجهة المخصص للمهمة
المسار المستهدف:
`lib/features/subscriptions/presentation/`

### فحص محتويات المجلد قبل التنفيذ:
- `tokens/presentation_tokens.dart`: تم إنشاؤه في المهمة السابقة `C-15` ليصدر كافة الرموز التصميمية (`AppTokens`).
- `screens/.gitkeep`: مجلد الشاشات فارغ تماماً باستثناء ملف الحفظ `.gitkeep`.
- `widgets/.gitkeep`: مجلد المكونات فارغ تماماً باستثناء ملف الحفظ `.gitkeep`.
- `state/.gitkeep`: مجلد إدارة الحالة فارغ تماماً باستثناء ملف الحفظ `.gitkeep`.
- `formatters/.gitkeep`: مجلد المنسقات فارغ تماماً باستثناء ملف الحفظ `.gitkeep`.

---

## 2. مسار اختبارات الواجهة
المسار المستهدف:
`test/widget/presentation/`
- المجلد غير موجود مسبقاً (تم فقط تجهيز `test/widget/core/theme/theme_accessibility_test.dart` في المهمة `C-15`).

---

## 3. حالة الاختبارات والتحليل الساكن
- إجمالي اختبارات المشروع الحالية: **150 اختباراً** (جميعها ناجحة 100%).
- حالة التحليل الساكن (`flutter analyze`): صفر أخطاء، صفر تحذيرات، صفر ملاحظات.
- شاشة البداية الحالية في `lib/main.dart` تعرض نصاً تجريبياً بسيطاً (`Scaffold` بنص «Sub Tracker — متتبع الاشتراكات»).
