# الملاحظات الحرجة والديون التقنية (Important Notes)

## 1. التبعيات والحدود المعمارية الحساسة

1. **نقاء طبقة الدومين (Zero-Tolerance Domain Purity):**
   - يُمنع تماماً استيراد أي مكتبة من خارج Dart النقي داخل `lib/features/*/domain/` أو `lib/core/usecase/` أو `lib/core/error/`.
   - يتم التحقق من هذا الشرط تلقائياً في سير عمل GitHub Actions، وأي مخالفة توقف البناء فوراً.
2. **استقلالية الملفات ومبدأ المسؤولية الواحدة (SRP):**
   - كل صنف (Entity, ValueObject, Failure, UseCase, Repository Contract) يجب أن يكون في ملف منفصل ومستقل.
   - يُمنع دمج واجهة المستودع مع تنفيذه، أو دمج الـ UseCase مع الـ State، أو دمج الـ Validator مع الـ Entity.
3. **عقد معالجة الأخطاء (Error Handling Contract):**
   - يُمنع إطلاق الاستثناءات (`throw`) للتعامل مع الأخطاء المتوقعة.
   - تستخدم كافة الـ UseCases النمط الوظيفي `Result<T>` (`Success<T>` أو `Error<T>`).
   - تتولى طبقة البيانات تحويل كافة استثناءات التخزين إلى كائنات `Failure` عند حدود المستودع.

---

## 2. إدارة Git والفروع ومراجعة الكود

1. **الفروع الرئيسية المحمية:**
   - الفرعان `main` و `develop` محميان بصرامة.
   - يُمنع الدفع المباشر (`direct push`) إليهما إطلاقاً.
   - لا يتم الدمج إلا عبر طلب دمج (Pull Request) معتمد يجتاز كافة فحوصات CI الآلية.
2. **فروع الميزات (Feature Branches):**
   - يتم العمل دائماً في فروع ميزات مخصصة للمهمة (مثال: `feature/c-09-repository-contracts-and-usecases` أو `feature/task-xxx`).
3. **توزيع المسؤوليات وملاك الشيفرة (CODEOWNERS):**
   - **أواب النزيلي (Awab Alnozili):** طبقة الدومين والعقود ومحركات الأعمال والواجهات (`domain/`, `presentation/`).
   - **قحطان الشاجع:** المعمارية والحوكمة والتوثيق وملفات الوكيل.
   - **محمد العيدروس:** الدومين ونظام التصميم والواجهات.
   - **مشعل حاجب:** التخزين المحلي وقواعد البيانات والجداول.
   - **محمد العواضي:** ضمان الجودة ومنظومة الاختبارات وحالات الحافة.

---

## 3. حالة المشروع الحالية في لوحة كانبان (Kanban Status)

| البطاقة | الوصف | الحالة | الملاحظات |
|---|---|---|---|
| **C-01** | تأسيس المستودع وهيكل المجلدات | **Done** | تم تأسيس الهيكل النظيف |
| **C-02** | حماية الفروع وقواعد الدمج | **Done** | تفعيل الحماية |
| **C-03** | ملفات الوكيل وقواعد SOLID وResult | **Done** | اعتماد `codeguaid.md` و`Result<T>` و`UseCase` |
| **C-04** | اعتماد الوثائق ومصفوفة التتبع RTM | **Done** | ترقيم 144 حالة حافة و40 قصة |
| **C-05** | مخطط Drift والوحدات الصغرى والترقية | **Done** | توثيق الجداول والسياسات |
| **C-06** | خط الأنابيب المستمر CI وبوابات الجودة | **Done** | ضبط سير عمل GitHub Actions |
| **C-07** | الكيانات وكائنات القيمة في الدومين | **Done** | 60 اختبار وحدة وتغطية >96% |
| **C-08** | قواعد التحقق وحساب التقويم وكشف التكرار | **Done** | 132 اختبار وحدة وتغطية >94% |
| **C-09** | **واجهات المستودعات وحالات الاستخدام** | **Ready for Dev** | **المهمة التالية في مسار الدومين** |
| **C-10** | **المحرك الحسابي المالي والإجماليات** | **Ready for Dev** | **تلي C-09** |
| **C-11 إلى C-14** | التخزين المحلي SQLite/Drift والـ DAOs | Backlog / Ready | مسار البيانات (مشعل حاجب) |
| **C-15 إلى C-17** | نظام التصميم والواجهات والتفاعل | Backlog / Ready | مسار الواجهة (محمد العيدروس / أواب النزيلي) |
| **C-18** | منظومة الاختبارات الشاملة وحالات الحافة | Backlog | مسار الجودة (محمد العواضي) |

---

## 4. الديون التقنية والملاحظات الهندسية (Technical Debt & Caveats)

1. **ملف `pubspec.yaml` الحالي:**
   - يحتوي فقط على مكتبات Flutter الأساسية و `flutter_lints`.
   - عند الشروع في تنفيذ طبقة التخزين الفعلي (Drift)، سيتم إدراج حزم Drift ومولد الشيفرة `build_runner` في فرع البيانات الخاص بـ (C-11) دون المساس بطبقة الدومين الحالية.
2. **المجلدات الحالية الفارغة ذات ملف `.gitkeep`:**
   - تم تجهيز هيكل الطبقات مسبقاً (`repositories/`, `usecases/`, `data/`, `presentation/`) بملفات `.gitkeep` للحفاظ على الشجرة في Git، وهو ما يسهل ملء الملفات دون الحاجة لإعادة هيكلة المشروع.
3. **تطابق البيئة المحلية:**
   - التطبيق يتطلب Dart SDK 3.x مع Flutter Stable.
   - الفحوصات الصارمة تتطلب خلو الكود من أي تحذير (`flutter analyze --fatal-infos`).
